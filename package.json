{
  "name": "mv-task-server",
  "version": "0.0.1",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 5000",
    "dev": "json-server --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
